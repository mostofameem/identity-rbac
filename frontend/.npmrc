# react-scripts@5 declares peerOptional typescript "^3.2.1 || ^4", which
# conflicts with the typescript ^5 this project pins. The peer is optional
# and TS 5 works fine with CRA, so relax the resolver instead of downgrading.
legacy-peer-deps=true
